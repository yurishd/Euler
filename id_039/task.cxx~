#include <iostream>
#include <set>
#include <deque>
#include <fstream>
#include <algorithm>
#include <iterator>

using namespace std;

int main(int argc, char* argv[])
{
  long long my_number = 2;

  for (int i = 1; i < 7830457; ++i) {
    my_number *= 2;
    while (my_number > 10000000000) {
      my_number -= 10000000000;
    }
  }
  cout << my_number << ":" << my_number * 28433 + 1 << endl;
  long result = 0;
  cout << result << endl;
  return 0;
}
