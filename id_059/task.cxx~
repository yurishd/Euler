#include <iostream>
#include <vector>
#include <fstream>

using namespace std;

int main(int argc, char *argv[])
{
  int sum = 0;
  ifstream is(cipher1.txt);
  vector<char> message;
  int itmp;
  is >> itmp;
  while (is) {
    message.push_back((char)itmp);
    is >> itmp;
  }
  message.push_back((char)itmp);


  cout << sum << endl;
  return 0;
}
