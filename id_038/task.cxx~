#include <iostream>

using namespace std;

int main(int argc, char* argv[])
{
  char test[10];
  long resultMaz = 0;
  for (long i = 1; i < 500000000; ++i) {
    long tmp  = i;
    memset(test, 0, 10);
    int count = 0;
    while(1) {
      long tmp1 = tmp;
      bool bOk = true;
      while (tmp1 > 0) {
        long mod = tmp % 10;
        if (!mod || test[mod]) {
          bOk = false;
          break;
        }
        test[mod] = 1;
        tmp /= 10;
        ++count;
      }
      if (!bOk) break;
      tmp *= i;
    }
    if (0 == i % 10000) {
      cout << i << endl;
    }
  }
  cout << "DONE" << endl;
  return 0;
}

